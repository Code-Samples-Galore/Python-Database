[pytest]
# The test modules import `database` / `main` from the repository root. Under
# pytest's default "prepend" import mode only tests/ lands on sys.path, so
# `pytest tests/` would fail with ModuleNotFoundError without this.
pythonpath = .
testpaths = tests
